/**
 * The SecondIntImpl class is an implementation of the SecondInterface.
 * It provides a default implementation for the doAction() method.
 */
public class SecondIntImpl implements SecondInterface {
    /**
     * Performs the default action defined by the SecondInterface.
     * Prints "The second interface says hello!" to the console.
     */
    public void doAction() {
        System.out.println("The second interface says hello!");
    }
}
